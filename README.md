# riddle-or-joke
## Client
To start client dev server:
````
cd client
npm install
npm run dev (keep open)
````
## Server
To start server:
````
cd server
npm install
npm run dev (keep open in different cmd)
````
